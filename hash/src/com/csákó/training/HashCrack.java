package com.csákó.training;
/*
 * package com.epam.training;
 * 
 * public class HashCrack {
 * 
 * private String result; private String hash;
 * 
 * public HashCrack(String hash) { this.result = ""; this.hash = hash; }
 * 
 * public String nextWord(String currentWord) { char[] nextWord =
 * currentWord.toCharArray(); char[] helperWord; for (int i = nextWord.length -
 * 1; i >= 0; i--) { if (nextWord[i] != 'z') { nextWord[i]++; for (int j = i +
 * 1; j < nextWord.length; j++) { nextWord[j] = 'a'; } } else if (i == 0) {
 * helperWord = new char[nextWord.length + 1]; helperWord[0] = 'a'; for (int j =
 * nextWord.length - 1; j >= 0; j--) { helperWord[j + 1] = nextWord[j]; }
 * nextWord = helperWord; } }
 * 
 * return String.valueOf(nextWord); } }
 */
